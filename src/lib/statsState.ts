import type { MetaStats } from "@/types/types";

export const statsStartState: MetaStats = {
  totalTicks: 0,
  totalDays: 0,
  totalPlayHours: 0,
  totalPlayMinutes: 0,
  totalPlaySeconds: 0,
  totalPlayMilliseconds: 0,
};